# Performance Test Instructions

## Status: N/A (수행하지 않음)

**Rationale**: 요구사항(NFR-1)상 문서 규모가 수십 개 이하이고, 내부망/로컬 단일 사용자 환경으로 성능 요구사항이 정의되지 않았다. 요청 시 파일 스캔 방식으로 충분하며, 성능 테스트는 가치가 없다.

## 향후 필요 시점
다음 상황이 되면 성능 테스트를 도입한다:
- 문서 규모가 수천 개 이상으로 증가 (→ 인덱스 도입 검토와 함께)
- 다수 동시 사용자/에이전트가 사용
- 시맨틱 검색(임베딩) 도입으로 검색 비용이 커질 때

도입 시 권장 도구: `hey` 또는 `k6`로 `/mcp` 엔드포인트에 search 호출 부하 측정.
